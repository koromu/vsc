#include <iostream>
#include <string>
#include <algorithm>

#define int long long
using namespace std;
typedef pair<int, int> pii;

const int N = 1010;


void solve()
{       
    char map[N][N];
    int n, m;cin >> n >> m; 
    for(int i = 0 ; i < n ; i ++)
        cin >> map[i];
    // 读入地图以后，然后
}

signed main()
{
    int _ = 1;
    while(_--) solve();
    return 0;
}
