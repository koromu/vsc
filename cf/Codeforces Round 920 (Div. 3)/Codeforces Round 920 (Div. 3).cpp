#include <iostream>

using namespace std;

const int N = 1e5 + 10;

void solve()
{
    int a[N], p[N][], b[N][];//q是后缀和, b是后缀和的后缀和, 第一个参数是步长, 第二个参数是
    int n, q;cin >> n >> q;
    for(int i = 1 ; i <= n ; i ++) cin >> a[i];

    //夭折, 看懂了后缀和和后缀和的后缀和, 但是看不懂跳跃后缀和

}


int main()
{
    int _;
    cin >> _;
    while(_--)
    {
        solve();
    }

    return 0;
}