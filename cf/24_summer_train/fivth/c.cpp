#include <iostream>
#include <string>
#include <algorithm>

#define int long long
using namespace std;
typedef pair<int, int> pii;

const int N = 200010;
int w[N];
int ans[N];
int fa[N];

void solve()
{
    // 打暴力, 根据k, 确定每一个起点, 然后判断两边
    // 从上往下遍历就行了
    // 最后判断是不是所有的点都遍历到了
    return;
}


signed main()
{
    int _;cin >> _;
    while(_--)
    {
        solve();
    }
    return 0;
}
