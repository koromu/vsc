#include <bits/stdc++.h>
using i64 = long long;
using namespace std;

const int P = 1e9 + 7; 
const int N = 200010;

int dp[N];

void solve()
{
    int n, k; cin >> n >> k;
    // 贪心嘛, 狠狠的贪
    int ans = 0;
    for(int i = 1 ; i <= n ; i ++)
    {
        int t, a, b; 
        cin >> t >> a >> b;
        
    }
}


signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t = 1;
    // cin >> t;
    while(t--)
        solve();

return 0;
}