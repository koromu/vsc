#include <iostream>
#include <string>

using namespace std;

const int N = 100010;

string w[N];
int main()
{   
    int n;
    cin >> n ;
    for(int i = 0 ; i < n ; i ++) cin >> w[i];

    // 最少要删除多少个数, 使得变成一个接龙数列
    //f[i][j]表示从前i个中选, 
    return 0;
}